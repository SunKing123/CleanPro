<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <solid android:color="@color/permission_add_qq" />

    <!-- 圆角的幅度 -->
    <corners android:radius="@dimen/dimen_33dp" />
    <stroke android:color="@color/permission_add_qq_stroke"
        android:width="2px"/>

</shape>
